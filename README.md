## Per collegarsi all'alphabot in ssh:
ssh pi@AB06.local
password: raspberry

## Per crearsi l'env, usare:
python3 -m venv app_env
(chiamare l'env così)

**Avviare l env e fare:**
pip install -r requirements.txt